custom:
  broker-addr: 192.168.119.132:8081    #接入点地址，需要设置成Proxy的地址和端口列表，一般是xxx:8081;xxx:8081。
server:
  port: 7020
spring:
  cloud:
    nacos:
      server-addr: localhost:8848
  application:
    name: country-mq
rocketmq:
  name-server: 192.168.0.110:9876
  producer:
    group: producerGroup1
    compress-message-body-threshold: 4096
    retry-next-server: true